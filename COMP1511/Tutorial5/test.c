#include <stdio.h>

int sum(int a, int b);

int main (void)
{
	int a = 1;
	int b = 2;
	
	
	int c = sum(a,b);
	printf("%d %d", c, d);
	return 0;
}

int sum(int a, int b)
{
	int d = b * 2 / a;
	return 0;
}
